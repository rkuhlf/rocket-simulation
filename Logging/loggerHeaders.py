# A metaclass that accepts an array of dictionaries of header names and what they will eventually be written as
# creates a class that has all of those key value pairs as static values
# Also creates a bunch of methods that have notImplementedErrors for every single one of those header names


